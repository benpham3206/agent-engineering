# CI/CD standard

CI should progress from fast deterministic checks to integration and behavioral evaluation. Deployment must be explicit, observable, and reversible. Centralize organization-wide policy when duplication across many repositories becomes a maintenance problem.
