# Deployment

Deployment is enabled for this repository. Implement `scripts/project/deploy` for each supported environment, keep production credentials outside Git, and define a tested rollback path to a previous known-good artifact.
