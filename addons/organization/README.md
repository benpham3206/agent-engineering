# Organization add-on

Use this add-on for repositories operated by internal teams. It adds service ownership, RFC/runbook/incident documentation, and operational directory structure without adding public governance or licensing files.

Only files under `files/` are copied into generated repositories.
