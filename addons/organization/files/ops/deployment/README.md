# Deployment operations

Document deployment ownership, promotion policy, rollback expectations, and required evidence. Enable the `deployment` add-on when this repo directly owns deployment automation.
