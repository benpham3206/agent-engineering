# Benchmarks

Keep reproducible performance/capability benchmark definitions here. Generated benchmark outputs belong in `artifacts/benchmarks/` unless intentionally promoted to a versioned baseline.
