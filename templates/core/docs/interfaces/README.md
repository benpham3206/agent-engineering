# Interfaces

Document contracts that cross component boundaries and are relied on by multiple subsystems.

Each interface document should define inputs, outputs, validation, errors, retries/idempotency where relevant, trust assumptions, and compatibility/versioning expectations.
